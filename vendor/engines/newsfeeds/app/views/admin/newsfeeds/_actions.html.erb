<ul>
  <% if Admin::NewsfeedsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_newsfeeds_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_newsfeed_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::NewsfeedsController.sortable? and Newsfeed.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Newsfeeds"),
                 admin_newsfeeds_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Newsfeeds"),
                 admin_newsfeeds_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>