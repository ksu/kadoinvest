<ul>
  <% if Admin::NewsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_news_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_new_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::NewsController.sortable? and New.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "News"),
                 admin_news_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "News"),
                 admin_news_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>